{
  "name": "cf-imgbed-mcp",
  "version": "0.1.0",
  "description": "Cloudflare Workers image hosting with built-in MCP server (R2 + Hono + Cloudflare Access)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.13.3",
    "hono": "^4.12.0",
    "jose": "^6.2.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
